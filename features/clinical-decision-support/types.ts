/**
 * Host-facing type facade. Clinical contracts live in the private care package
 * so rule implementations and their result types are released together.
 */
export type * from '@voho0000/personalized-care'
