// Last-resort compatibility shim for an on-prem import that has not yet moved
// behind a deployment adapter. No Firebase package is imported or initialized.
export const app = undefined
export const auth = undefined
export const db = undefined
export const appCheck = undefined
