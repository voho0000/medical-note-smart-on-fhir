# 專案健檢 2026-08-15 — 安全性 × 臨床流暢 × 效能

> 基準：working tree @ `93d7870e`（v0.48.0 之後）。三條並行深度審查（安全、臨床 UX、效能），每項發現都已對現行程式碼逐一驗證，非文件轉抄。情境設定：繁忙門診／查房、老舊院內 PC 與 iPad、院內 Wi-Fi 不穩。
>
> 編號規則：S=安全、U=臨床 UX、E=效能。已知且已決策的事項（vghbrain gateway）不重複列為 finding。

## 已知事項（不重複討論）

- **vghbrain 解密金鑰明文在前端**：`src/application/launch/medcloud-launch-context.ts:19-20`（`VGTPE_MEDCLOUD_DECRYPTION_KEY_BASE64URL`）。等後端 gateway 方案，此處僅記錄位置。周邊的 postMessage 驗證（`medcloud-launch.provider.tsx:109-113`：`event.source === window` + exact origin + schema + messageId 去重 + pagehide 清除）本身是嚴謹的。

---

## 優先序總表

### 第一波（小改動、高回報）— ✅ 已於 2026-08-15 實作（S2 除外）

| ID | 面向 | 一句話 | 狀態 |
|----|------|--------|------|
| S1 | 安全 | 登出沒清 SMART token，共用工作站「登出」後病歷仍可讀可抓 | ✅ 已修 |
| S2 | 安全 | live Resend API key 躺在 Google Drive 同步資料夾的 `.env.local` | ⚠️ 待你輪替（只能人工做） |
| U1 | UX | 開趨勢圖會 unmount 整個右側面板：聊天串流中斷、計算機輸入全失、IPS 確認清空 | ✅ 已修 |
| U2 | UX | 聊天錯誤無重試鈕、已串流的部分回答被整段換成 ❌ | ✅ 已修 |
| E1 | 效能 | recharts 進了初始 bundle，但圖表只在點擊後才開 | ✅ 已修 |
| E2 | 效能 | ReportInterpretationPanel 靜態 import 把 react-markdown+zod 拖進首屏 | ✅ 已修 |
| E3 | 效能 | `inferredUnitInHeader` 每次 render 掃全表 cell，未 memo | ✅ 已修 |

**實測結果**（`npm run build:gh`，同一 commit 前後對照）：首屏執行的 JS 由 **4,649KB raw / 1,322KB gzip → 4,094KB / 1,164KB**，即 **-555KB raw（-11.9%）、-158KB gzip（-12.0%）**。
（審查初估的「-35%」是拿 8/13 的舊 `out/` 當基準算的，該 baseline 早於近期數個 commit，不成立；上面是乾淨 A/B 的真實數字。）

實作備註：
- S1 新增 `clearSmartSession()`（`fhir-client.service.ts`），清 `SMART_KEY` 指標＋其指向的 token entry＋in-memory client，於 `signOut` 呼叫。
- U1 右側改為 CSS 隱藏（`detail ? 'hidden' : 'contents'`）而非條件式 render；計算機**不加** registry `forceMount`（那會讓 57 計算機 chunk 提前下載），改在 `RightPanelLayout` 加「造訪過的 tab 保持掛載」的通用機制，狀態一樣保住但 chunk 仍延後載入。
- U2 `ChatMessage` 新增 `error?: string`；失敗時保留已串流內容，錯誤改以 `role="alert"` 橫幅＋重試鈕呈現；`retryLastMessage` 會移除失敗的那組 user+assistant 訊息再重送（用 token bump 避開 `handleSend` 的 stale closure）。輸入框仍在送出時清空（維持手感），但重試已讓內容可完全復原。
- 順手修掉 U7 的一部分：趨勢/歷史 icon button 在手機補足 44px 觸控目標。
- 驗證：`npx tsc --noEmit` 乾淨、`npm test` 3,365 passed、lint 無新增警告；瀏覽器實測趨勢圖延遲載入、計算機狀態跨趨勢開關保留、聊天失敗→重試成功且未重複發問。

### 第二波（中等工程量，臨床體感明顯）

| ID | 面向 | 一句話 | 狀態 |
|----|------|--------|------|
| U3 | UX | 手機鍵盤遮住輸入框（無 visualViewport 處理）＋ input 字級 <16px 造成 iOS 聚焦自動縮放跳動 | ✅ 已修 |
| U4 | UX | 計算機 autofill 分不出「載入中/錯誤」和「病人真的沒有這個檢驗」 | ✅ 已修 |
| U5 | UX | 累積報告沒有 analyte 搜尋 | ✅ 已修 |
| U6 | UX | SMART 載入是 all-or-nothing：15 種 resource 一個 `Promise.all`，最慢的（Observation 序列翻頁）擋住全部卡片；FHIR request 無 timeout/AbortSignal，Wi-Fi 卡住＝無限「載入中」 |
| U7 | UX | 12px 點擊目標（趨勢/圖片按鈕）與 rem 陷阱殘留（min-h-11 在手機=33px）；民眾 audience 手機字級 9.75px（C1 仍未修） |
| S3 | 安全 | on-prem audit/sanitize 只掃 `out/_next/static`，但打包的是整個 `out/`（root HTML、public/ 都沒掃） |
| E4 | 效能 | Observation 翻頁 `_count=200` 嚴格序列（最多 50 頁）、vitals 又整包重抓一次 |

### 第三波（架構性／治理性，排程處理）

| ID | 面向 | 一句話 |
|----|------|--------|
| S4 | 治理 | 真病人聊天內容自動存 Firestore（美區、個人 Google 帳號專案、無 TTL、非 E2E 加密）——上真實 PHI 前要有機構決策；短解：real-SMART session 停用 auto-save |
| S5 | 部署 | cloud CSP `unsafe-inline` + `connect-src https:`（任意主機）；GH Pages 完全無 response headers；mediprisma.tw 是自控主機、可先補真 headers |
| S6 | 安全 | FHIR narrative 遠端圖片只在 on-prem 剝除，cloud 版會載入（tracking pixel 可回報開啟時間/IP） |
| E5 | 效能 | 累積報告 pivot 表全量 DOM（數百列×數十欄）＋看過的分類 `forceMount` 累積不釋放；空 cell 逐格 inline gradient |
| E6 | 效能 | 12MB NHI 藥品 terminology chunk 命中時是主執行緒一次 `JSON.parse`（已正確 lazy，但老機器仍會頓數秒） |
| U8 | UX | 26 處 hover-only tooltip 在觸控上看不到（截斷的檢驗全值、跨院機構名、報告全名）——截斷類要 tap-to-expand 而非搬 InfoHint |
| U9 | UX | 聊天紀錄資料遺失路徑：force-save 失敗仍丟棄對話、載入失敗無聲、stage-1 save 失敗只進 console |

---

## Part A — 安全性細節

### S1（HIGH）登出不清 SMART token
`signOut`（`src/application/providers/auth.provider.tsx:297-330`）清了 AI keys、session crypto key、匯入 bundle、AI 快取，但 fhirclient 的 `SMART_KEY` 指標與 token JSON（sessionStorage，`fhir-client.service.ts:55-62` 讀取）原封不動，`FhirClientService.clearClient()` 也沒被呼叫。共用工作站按登出後，分頁仍能以有效 token 繼續打 FHIR server 直到 token 過期。修法：signOut 內移除 `SMART_KEY` 與其指向的 entry、呼叫 `clearClient()`、導離病歷頁。

### S2（MEDIUM）Resend key 在 `.env.local`
`RESEND_API_KEY=re_...`。未進 git（已驗 `git ls-files` 與 `git log -S`），但 repo 在 `My Drive/` 下持續同步到個人 Google Drive。唯一消費者是 dev/Vercel 專用 `app/api/feedback/route.ts:215`。修法：輪替該 key，只放 Functions/Vercel secret store，從 `.env.local` 刪除。

### S3（MEDIUM）on-prem artifact 掃描範圍不足
`scripts/audit-onprem-artifact.mjs:5` 與 `scripts/sanitize-onprem-artifact.mjs:5` 都 root 在 `out/_next/static`，但 `scripts/package-onprem.mjs:48-54` tar 整個 `out/`。route HTML（含 inline bootstrap/RSC payload）、`public/` 複本、demo bundle 都不在 forbidden-domain 掃描範圍。修法：兩支 script 改 root 到 `out/`（沿用副檔名過濾）。

### S4（治理）Firestore 聊天持久化含真病人 PHI
`chat-session.repository.ts:48-94` 存完整訊息文字、`patientId`、`fhirServerUrl`、`replyTo.excerpt`（病歷原文摘句）；`useChatHistoryDrawer.ts:24` 預設 `persistenceEnabled ?? true`，SMART 真病人 session 也自動存。使用者訊息未 scrub（scrub 只做在 outbound-to-AI，設計如此），貼上的姓名/病歷號會落地 Firestore。SECURITY.md 已列為 residual risk——這是治理行動項而非未知漏洞。短解一行：對 real-SMART-issuer session 停用 auto-save（自訂 endpoint 已有相同前例）；或加 TTL＋機構帳號政策後再上真 PHI。

### S5（部署）CSP／headers 現況
- `app/layout.tsx:27-40` meta CSP（production only）：`script-src 'self' 'unsafe-inline'`、`connect-src 'self' https: wss:`（任意 HTTPS 主機）、`img-src … https:`。
- `next.config.ts:140-160` 的 headers（`frame-ancestors` allowlist 等）在 GH Pages／mediprisma 靜態鏡像不會送出（程式註解自己有寫）；`frame-ancestors` 無法用 meta 傳遞 → github.io 版無任何 framing 限制。
- 意涵：任一 HTML injection 立足點可升級為任意 script 執行並外送 PHI 到任意 HTTPS 主機。理由（static export inline scripts、任意 SMART iss）已記錄於 SECURITY.md；可行的殘餘行動：mediprisma.tw 補真實 response headers、考慮 script hashes。
- on-prem `deploy/intranet/Caddyfile.example` 很嚴，但 `script-src 'self'` 會擋 Next inline bootstrap——採用者可能私加 `unsafe-inline`；建議預算 hash 寫進範例。

### S6（LOW-MED）narrative 遠端圖片
`sanitize-narrative.ts:95-105` 只在 `DEPLOYMENT_CONFIG.isOnPrem` 剝除非同源 `<img>`；cloud 版照載（CSP `img-src https:` 放行）。Markdown/AI 輸出已正確擋圖（`MarkdownRenderer.tsx:49-53`）。修法：cloud 也剝除或 click-to-load。

### S7（LOW）npm audit
prod deps 4 high：`nanoid <3.3.18`（DoS，需 size-0 custom generator，無現實攻擊路徑）、`postcss`/`sharp`/`next`（static export 下皆 build-time）。方便時 `npm audit fix`。DOMPurify 已鎖 3.4.12（舊 audit A2 已解）。

### S8（驗證良好——不要重工）
- outbound PII scrub 真實存在且接得廣：`pii-text-scrub.ts` 用於 clinical context（`use-clinical-context.hook.ts:247`）、agent chat 三處、每個 FHIR tool 回應（`fhir-tools.ts:561`）、全部 summary/safety/insights/report/followup/title use-cases。
- iss allowlist + PKCE required + public client（`smart-issuer-policy.ts`、`app/smart/launch/page.tsx:79-89`）；on-prem fail-closed。
- XSS 面：所有 `dangerouslySetInnerHTML` 過 DOMPurify；react-markdown 無 rehype-raw；連結 `noopener noreferrer`；無 eval/new Function；`window.open` 有 opener 防護；IPS 列印路徑有 escape。
- 兩個 postMessage handler（medcloud、nhi-viewer）都驗 source+origin+schema。
- 無 analytics 呼叫；feedback 表單不收 patientId；URL 無 PHI；repo 內只有 genuinely-public 設定值。
- 本地 bundle AES-GCM + tab-scoped key + 12h TTL；登出清 AI 快取與 keys（除了 S1 的 SMART token 缺口）。

---

## Part B — 臨床 UX 細節

### U1（BLOCKER 級）右側 detail pane 以 unmount 方式切換
`app/page.tsx:302-309` 在開 lab 趨勢時把 `RightPanelFeature` 條件式換成 `RightDetailPane`（≥768px 時 `CumulativeLabReport.tsx:470-489` 走此路）。已驗證的後果：聊天串流在 unmount 時 abort（`useAgentChat.ts:173-179`）；計算機非 `forceMount`（`right-panel-registry.ts:100-106`）且值在 local `useState`（`Feature.tsx:49-51`）→ 填了 8 個欄位、瞄一眼 CRP 趨勢回來全空；IPS AI 推論問題與逐項醫師確認也清空（`useInferredProblems.ts:92-95`）。修法：改 CSS 隱藏（手機面板切換已用同 pattern：`workspace-layout.tsx:84` 的 `max-md:hidden`）。同因：計算機在一般右側 tab 切換也會歸零——`forceMount` 或把值提升到 per-patient store。

### U2（MAJOR）聊天錯誤處理
- catch 把 assistant 泡泡整段換成 `❌ <message>`（`useAgentChat.ts:663-669`），40 秒已串流的回答因 Wi-Fi 抖動蒸發；全 feature 無 retry 按鈕。
- 輸入框在 `handleSend` 前就清空（`MedicalChat.tsx:512`）；「首 chunk 才清」的 guard（`useAgentChat.ts:467-470`）是 dead code；user 泡泡沒有 copy 鈕。
- FHIR 資料載入中 send 鈕靜默 disabled 無提示（`MedicalChat.tsx:474-478` → `ChatInputArea.tsx:242`）；local-endpoint 模式同狀態還會誤報「context too large」（`useAgentChat.ts:404-406`）。
- idle watchdog 60s（`env.config.ts:36`）對門診節奏偏長，且全 app 無 wall-clock 上限（慢滴串流永不結束，只能手動 stop）。

### U3（BLOCKER on phone）鍵盤與字級
全 app 零 `visualViewport`／`interactive-widget` 處理；root `h-svh overflow-hidden` 無 bottom inset（`workspace-layout.tsx:23`）；`ChatExpandedOverlay.tsx:28` `fixed inset-0`。加上手機 root 自動降 12px（`font-size.provider.tsx:48`），所有 input 計算後 <16px（chat textarea 10.5px）→ iOS 每次聚焦自動縮放跳動，計算機 8 欄位流程最痛。修法：`interactive-widget=resizes-content`＋visualViewport CSS var；input 強制 `max-md:text-[16px]`；補 `pb-[env(safe-area-inset-bottom)]`。

### U4（MAJOR，臨床安全性質）計算機 autofill 無載入/錯誤語意
`features/medical-calculator/Feature.tsx:45,70-71` 不讀 clinical data 的 `isLoading`/`error`；載入期間每個計算機顯示未帶入、「此病人適用」計數為 0——空白可能被讀成「病人沒有此檢驗」。`autofill-compute.ts:144-146` 與 `CalculatorDetail.tsx:39-42` 吞掉計算例外 → 空白結果區無「無法計算」狀態。

### U5（MAJOR）累積報告缺 analyte 搜尋
報告搜尋列在 cumulative tab 明確隱藏（`ReportsCard.tsx:661`）；找單一 analyte = 猜分類＋橫向捲數十欄。`focusAnalyteKey` + 置中 effect（`CumulativeLabReport.tsx:133-146`）已存在，補一個 typeahead 接上即可。

### U6（MAJOR）載入模式與網路韌性
- 單一 React Query 包 15 種 resource 的 `Promise.all`（`use-clinical-data-query.hook.ts:33-84`），任何 data-bearing 卡片都等最慢者；`requestAllPages` 每型嚴格序列翻頁（`fhir-client.service.ts:152-169`，上限 50 頁）。`resourceQueryStatus` 已在追蹤——拆 per-type query 或 progressive settle 即可讓卡片先到先畫。
- FHIR request 無 timeout/AbortSignal；TCP 卡住＝無限載入；React Query `retry: 1` 會從第 1 頁重跑整條翻頁鏈。
- 已良好：15 路並行、單型失敗降級為 `[]`＋amber banner＋重試鈕（`FhirDataIssuesBanner`）、Reports tab idle 預熱、SMART callback 有真錯誤頁。

### U7（MAJOR on phone）觸控目標與民眾字級
- 趨勢/歷史 icon 無任何 sizing（`ObservationLongitudinalAction.tsx:100-113`）→ 手機 12px；報告圖片鈕同（`ReportRow.tsx:437-458`）。
- rem 陷阱殘留：`min-h-11`/`h-10`/`h-7` 在手機 root 12px 下= 33/30/21px——累積趨勢 header（`CumulativeLabReport.tsx:297`）、chat send（`ChatInputArea.tsx:234`）、隱私 toggle（`ChatToolbar.tsx:168`）。正確 pattern（`min-h-[44px] … lg:min-h-8`）已在 workspace tabs 使用，補齊即可。
- 民眾 audience：手機 root 12px 下 summary 本文 9.75px、免責 7.5px、citation pill 6.75px（MEDICAL-SUMMARY-AUDIT C1，仍未修）；民眾模式仍看得到 ModelPicker/設定（C5 仍開）。citation pill 命中區 `before:-inset-1.5` 是 rem-based，手機實際 ~22×18px，改 literal px。

### U8（MODERATE）觸控 tooltip
26 處 raw `TooltipTrigger`，最痛的是承載被截斷資訊者：檢驗全值與「未判讀」說明（`CompactLabResultRow.tsx:61-69,121-131`）、跨院機構全名（`ReportInstitutionLabel.tsx:24-41`）、報告全名（`ReportRow.tsx:170-176,882-889`）、NHI 藥品主檔（`MedicationTerminologyTooltip.tsx:60-81`）。截斷類正解是 tap-to-expand/popover，不是搬 InfoHint。另：聊天紀錄的刪除鈕在觸控上隱形但可誤觸（`ChatHistoryItem.tsx:70` `opacity-0 group-hover:opacity-100`）。

### U9（MODERATE）沉默失敗清單
- 聊天：New Chat force-save 失敗仍丟棄對話（`useChatHistoryDrawer.ts:137-142`）；載入舊 session 失敗無聲（`:71-73`）；stage-1 save 失敗只進 console（`MedicalChat.tsx:527-530`）。
- demo bundle fetch 無 timeout，掛住時歡迎頁兩顆按鈕永久 disabled（`useImportBundle.ts:177`）；同類：feedback submit、Perplexity proxy（openai/gemini 有 60s abort，它沒有）、Whisper 上傳。
- 無 retry 的死路錯誤：IPS export 載入錯誤卡（`features/ips-export/Feature.tsx:83-92`）、`personalized-education/LiveFeature.tsx:69-89` 四個分支。
- 剪貼簿失敗無回饋（`ReportRow.tsx:138,417`、`ReportInterpretationPanel.tsx:227-229`；`AiHandoffPanel` 有 toast，走共用 hook 即可）；`EmailVerificationBanner` 重寄/檢查失敗零回饋。
- 報告解讀：底層有串流但 UI 只轉 spinner、不能取消（`use-report-interpretation.hook.ts:176-183`）；錯誤 UI 丟棄 mapped cause，quota/auth 失敗也顯示「請稍後再試」（`ReportInterpretationPanel.tsx:122-137`）。
- 摘要區：safety 卡的 error+retry 管線掛著 `error={null}` 且無 `onRetry`（`Feature.tsx:686-693`）；context-overflow 版 `GenerationErrorBanner` 少了重試鈕（`GenerationErrorBanner.tsx:71-85`）。
- 資料過濾語意：`contextAdaptation` chip 只在系統自動縮 context 時出現；使用者自己的 資料選擇 縮小 AI 範圍時，summary/chat 無持續「已過濾」指示。

### 既有 audit 追蹤表（程式碼驗證後）
- MOBILE-TABLET-2026-06-14：鍵盤/sticky chat input 未做（=U3）；InfoHint 遷移停在 ~10 處（=U8）；pinch-zoom、768 分割已解。
- MEDICAL-SUMMARY-2026-07-12：C1 民眾字級未做（=U7）；C5 民眾看到 ModelPicker 未做；C2 citation 命中區部分完成；C8、C3、B2（catalog 尊重資料選擇）、B3（token budgeting 接活）、D1/D3 都已修好；B1 `latestAdmission` 預設仍在（老病摘預設不進 AI）；D4 `window.__safetyModelId` test seam 仍在 production 路徑（`use-ai-slot-generation.hook.ts:109`）。

---

## Part C — 效能細節

### 量測基準（out/ @ Aug 13）
- out/ 總量 38MB：`_next` 18MB＋`clinical-guidelines` 19MB（兩本 PDF 13MB+5.7MB，僅 on-demand fetch，屬部署體積問題）＋demo bundle 1.7MB。
- 首頁引用 23 個 chunk，**3,734KB raw / 1,059KB gzip**，全數啟動時 parse——老院內 PC 的首載主成本在 parse/compile。
- 最大 chunk 12.3MB（NHI 藥品 terminology `JSON.parse` 字串），已正確 dynamic import、不在首載（`nhi-drug-terminology-enrichment.service.ts:250`）。

### E1（HIGH，LOW effort）recharts 在初始 bundle
777KB chunk 指紋為 recharts；僅 3 檔用到（`CumulativeLabTrendDetail.tsx`、`ObservationTrendChart.tsx`、`MultiLineTrendChart.tsx`），全被急載的 reports workspace 靜態 import（`CumulativeLabReport.tsx:29-32`），但趨勢 UI 只在點擊後 mount。三個 `next/dynamic` 包裝約省 500-600KB raw（~150KB gz）＋parse。

### E2（HIGH，LOW effort）ReportInterpretationPanel 拖 react-markdown+zod 進首屏
`ReportRow.tsx`/`DocumentSummaryCard.tsx` → `ReportInterpretationPanel` → `MarkdownRenderer`（158KB chunk）＋`report-interpretation.entity.ts`（zod，346KB chunk）。功能本來就是一鍵 on-demand，lazy 化即可把兩條 graph 移出首屏。

### E3（TRIVIAL）`inferredUnitInHeader` 未 memo
`CumulativeLabReport.tsx:174-186` 每次 `LabPivotTable` render 掃全部欄×列（父層 tab-measure/focus/dialog state 都會觸發），`useMemo` keyed on `pivot` 即可。

### E4（MED）Observation 抓取策略
`_count=200` 序列翻頁（`clinical-data.repository.ts:344`；DiagnosticReport 已用 500），數千筆= 10-50 個序列 round-trip，主導整體載入時間（也是 U6 的根因之一）。且 `fetchObservations`（全類別）與 `fetchVitalSigns`（`category=vital-signs`，`:361`）重疊——vitals 下載＋mapping 兩次。修法：Observation 提到 500、vitals 改 client-side 從全集導出。

### E5（MED-HIGH on old PC）累積報告 pivot 表
全量 DOM：年單位資料的 chemistry 分類可達數百 date-rows × 20-60 欄=數萬 `<td>`（`CumulativeLabReport.tsx:326-372`）；看過的分類 `forceMount` 累積（`:649`，程式註解說是刻意 trade-off，但無上限）；空 cell 逐格 fresh inline `repeating-linear-gradient`（`EmptyCell`，`:86-88`）弱 GPU 痛。選項：date-rows 虛擬化（`@tanstack/react-virtual` 已安裝、raw report list 已在用）、首載限列數＋「顯示更早」、或超過門檻時逐出非 active 分類。

### E6（LOW 頻率）12MB terminology chunk
已正確 async 且 gated；SDK import 路徑在 worker（`sdk-import.worker.ts:9-10`），但 `local-bundle.service.ts` 路徑仍可能主執行緒命中：2.1MB gz 下載＋~11MB 字串 `JSON.parse`，老硬體數秒 jank。長期：改 fetch 靜態 JSON（可分段/裁剪）。

### E7（LOW）context value 穩定性
`language.provider.tsx:40`、`audience.provider.tsx:40` 每 render 新物件（目前近 root、安靜，低優先）；`right-detail.provider` 已正確 memo。

### 驗證良好（不要重工）
近兩個效能 commit（`1805214a`、`93d7870e`）屬實：tab-count 移出 render path、Reports idle 預熱、per-category 延遲掛載＋loading、趨勢可用性併入 O(n) pivot pass。另已良好：raw report list 虛擬化、文件 base64 lazy getter＋WeakMap、匯入 parse 在 Web Worker、chat 輸入 state 局部化、clinical-context hooks 皆 memo、`useClinicalData` 快照參照穩定、右側 features 皆 `next/dynamic`（含 57 計算機）、Firebase 全 lazy、無 barrel 濫用、`buildLabPivots` 單趟 O(n) 且 memo、React Query 不跨 reload 持久化（PHI 姿態正確）。

---

## 建議執行順序（單人開發節奏）

1. ~~**半天內的一批**：S1、E3、E1+E2、U1、U2~~ ✅ **已完成並 commit（`970bb433`）**。
2. ~~U4（calculator 載入語意）、U5（analyte 搜尋）~~ ✅ **已完成 2026-08-15**。S2（輪替 Resend key）與 S3（on-prem audit 範圍）依使用者決定暫不處理。
3. ~~U3（手機鍵盤+16px）、U6/E4 的 timeout＋`_count`、U7 觸控目標、C1 民眾字級~~ ✅ **已完成 2026-08-15（第三波）**。
4. ~~E5（pivot 虛擬化）、E6（terminology worker）、U8（觸控 tooltip 主要案例）、U9（沉默失敗全批）、U6 progressive per-type 載入~~ ✅ **已完成 2026-08-18（第四波，Opus agents 實作＋監工整合）**，另含：聊天雲端模型切換不再清空對話（runtime identity 邊界修正，SECURITY.md 同步更新）、demo Forxiga 測試的時間衰減修復（同 DEMO_DATA_AS_OF_MS 方法）、C5 撤銷（民眾保留模型選擇，功能不減）。剩餘決策項：S4（Firestore PHI 治理）、S5（mediprisma.tw headers）、S6（narrative 遠端圖片，待決定）、S2（Resend key 輪替）、B1（documentMode 預設）。

### 第三波實作備註（2026-08-15）

- **U3**：`Input`／`Textarea`／聊天輸入框／各處搜尋列補 `max-md:text-[16px]`（rem 在手機是 12px root，一律低於 iOS 的 16px 不縮放門檻）。viewport meta 加 `interactive-widget=resizes-content`（Chrome/Android），另建 `use-visual-viewport.hook.ts` 以 `visualViewport` 發布 `--app-viewport-height` 與 `--keyboard-inset` 供 iOS 用；workspace root 改吃該變數並補 bottom safe-area，聊天全螢幕覆蓋層預留鍵盤高度。
- **U6**：`fhir-client.service.ts` 新增 `requestWithTimeout`（30s，AbortController 而非 `AbortSignal.timeout` 以相容舊版院內瀏覽器），單頁與翻頁鏈每一頁都受限；逾時丟 `FhirRequestTimeoutError` 而不是永遠不 resolve。demo bundle fetch（30s）與 Perplexity（60s，對齊其他 AI service）補上逾時。
- **E4**：Observation 與 vital-signs 的 `_count` 由 200 提到 500（對齊 DiagnosticReport），減少序列翻頁次數。vitals 重複抓取未動——那需要改資料來源語意，風險高於效益，留待與 progressive 載入一起做。
- **C1**：手機自動降到 12px root 的規則改為看 audience——醫療人員維持 12px（密集表格），民眾維持 16px。`FontSizeProvider` 在 `AudienceProvider` 之上，故以 `AUDIENCE_CHANGED_EVENT` 通知而非 context。實測：medical 12px／patient 16px，所有輸入欄位 ≥16px。
- **U7**：報告影像鈕、聊天附件鈕、隱私切換補 44px 觸控目標（`max-md:` 前綴，桌機維持精簡）。

**Demo 引用失效（已於 2026-08-17 修復）**：`demo-ai-snapshots.test.ts` 原有 1 項失敗。追查後**成因與原先假設不同**——不是 bundle 編輯造成的 key 位移（catalog 的 M1–M21 對應 resource id 與既有 map 完全一致），而是**時間衰減**：預設 `medicationStatus: 'active'` 以 `Date.now()` 判斷，demo bundle 的 2026-07-20 調劑（28 天供應，結束日 08-16）在 08-17 當天過期，130 筆用藥只剩 15 筆在 AI 範圍內，六個引用（M16–M21）因此失效；07-21 那批隔天也會跟著掉。當時 AI 範圍比左側用藥卡片（顯示 20 筆使用中）更嚴格，這正是引用指不到東西的原因。

修法：新增 `src/shared/constants/demo-data.constants.ts` 的 `DEMO_DATA_AS_OF_MS`（2026-08-12，bundle 最新事件日）與 `clinicalNowMs(isDemo)`；`use-clinical-ai-input.hook.ts` 在 `isDemoDataActive()` 時把它傳給兩處 `scopeClinicalDataForAi`，真實病人仍用真實時鐘。測試改用同一個常數，不再隨執行日期漂移，並新增兩個守門測試（as-of 必須跟著 bundle 最新事件走、任何被引用的 key 都不得落在範圍外）。

### U4／U5 實作備註（2026-08-15）

**U4**：`useLabAutofill` 改回傳 `{ autofill, isLoading, error, retry }`（原本只取 `observations`，看不到 `useClinicalData` 已提供的狀態）。三個消費點分別呈現：
- 「此病人 N」晶片在載入中改顯示 pending 版本（原本 `relevantCount > 0` 讓它整個消失，讀起來像「沒有適用的計算機」）。
- 「為此病人」空清單分成 載入中／讀取失敗／確實沒有 三種文案。
- `CalculatorDetail` 加載入與錯誤橫幅（含重試），且只在該計算機真的取用病人資料時才顯示（問卷型計算機保持安靜）。
- **順帶修掉一個實質 bug**：`values` 只在掛載時 seed 一次，所以在資料載入完成前打開計算機，欄位會永遠空白（「重新帶入」按鈕又因 `hasAnyAutofill` 為 false 而隱藏）。改為 autofill 更新時重新 seed，但不覆寫使用者已輸入的值。
- `safeCompute` 回傳 `crashed` 旗標，公式丟例外時顯示「無法計算」而非「請填入所有必要欄位」（後者對已填完欄位的使用者是錯誤訊息）。

**U5**：新增 `AnalyteSearchBox`（typeahead）放在累積報告分類頁籤上方。索引同時涵蓋顯示名稱、canonical 代碼與民眾/醫療版譯名，所以「Hb」「血色素」「CREA」都找得到；結果列標示分類與「（無資料）」。選取後切分類並寫入 focus，重用既有的 `focusAnalyteKey` 置中＋高亮機制（原本只有 AI 引用導覽會觸發）。AI 導覽與搜尋共用同一個 focus 狀態，後動作者優先。

測試：`analyte-search.test.ts`（6 項，含跨分類同名、去重、pinned-but-empty）、`autofill-loading-state.test.tsx`（4 項，含「載入中打開計算機、資料到達後補填」這個競態）。
