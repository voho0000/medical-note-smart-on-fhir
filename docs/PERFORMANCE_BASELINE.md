# MediPrisma 前端效能基準

> 最近量測：2026-08-19｜版本基準：v0.48.0 working tree｜資料：內建虛構示範病人

本文件記錄臨床工作區常用操作的可重複效能基準。數字用來辨識後續改動是否造成退化，不代表所有電腦、瀏覽器或院內網路都會得到完全相同的結果。

## 目前 p95

| 使用者操作 | 目前保守 p95 | 五輪典型值 | 自動測試門檻 | 結果 |
|---|---:|---:|---:|---|
| 資料載入中首次切換左側主分頁 | **96.7 ms** | 81.4 ms | < 500 ms | 通過 |
| 暖機後切換左側主分頁 | **100.4 ms** | 99.7 ms | < 500 ms | 通過 |
| 同日背景恢復後首次切換主分頁 | **82.4 ms** | 81.4 ms | < 500 ms | 通過 |
| 累積報告開啟右側檢驗趨勢 | **208.9 ms** | 206.7 ms | < 500 ms | 通過 |
| 關閉右側檢驗趨勢 | **183.4 ms** | 166.4 ms | < 350 ms | 通過 |
| 用藥清單連續捲動影格間隔 | **17.6 ms** | 17.5 ms | < 80 ms | 通過 |

「保守 p95」採五輪測試中最慢的一次 p95；「典型值」是五個單輪 p95 的中位數。這樣不會只挑最快的一輪當成基準。

另外兩項防退化指標：

| 指標 | 五輪觀察值 | 自動測試門檻 | 結果 |
|---|---:|---:|---|
| 最長主執行緒工作 | 86–147 ms | < 300 ms | 通過 |
| 同日背景恢復產生的最長工作 | 0 ms | < 300 ms | 通過 |
| 重複切換後增加的 DOM 節點 | -1 | ≤ 150 | 通過 |

主執行緒工作表列的是單次最長值，不是 p95，因此不與上方 p95 混用。17.5 ms 的典型捲動影格約略接近 60 Hz 畫面的 16.7 ms 影格預算；目前沒有秒級停頓，但仍值得持續追蹤。

## 五輪原始結果

每輪包含 1 次資料載入中首次分頁切換、15 次暖機後分頁切換、1 次同日背景恢復後切換、5 次累積報告趨勢開啟、5 次趨勢關閉與 30 個用藥捲動影格；五輪合計 5 次載入中切換、75 次暖機切換、5 次背景恢復切換、25 次趨勢開啟、25 次趨勢關閉、150 個影格。

| 輪次 | 載入中切換 | 暖機切換 p95 | 背景恢復切換 | 趨勢開啟 p95 | 趨勢關閉 p95 | 捲動影格 p95 | 最長主執行緒工作 | DOM 增長 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 96.7 ms | 99.7 ms | 82.1 ms | 139.9 ms | 166.8 ms | 17.4 ms | 93 ms | -1 |
| 2 | 78.8 ms | 100.4 ms | 66.1 ms | 206.7 ms | 166.4 ms | 17.6 ms | 140 ms | -1 |
| 3 | 87.0 ms | 99.7 ms | 82.4 ms | 207.0 ms | 183.4 ms | 17.4 ms | 147 ms | -1 |
| 4 | 81.4 ms | 100.2 ms | 81.0 ms | 208.9 ms | 149.7 ms | 17.6 ms | 140 ms | -1 |
| 5 | 80.0 ms | 99.7 ms | 81.4 ms | 156.3 ms | 166.2 ms | 17.5 ms | 86 ms | -1 |

## 量測情境

- macOS 26.6、Apple Silicon（arm64）
- Node.js 24.12.0
- Playwright 1.62.1、headless Desktop Chrome
- `next dev --turbopack`，本機 `localhost:3001`
- 病歷來源是工作階段內固定的內建 Bundle 快照；不因經過時間、重新聚焦或網路恢復而重新解析，只有匯入、清除或手動重試會換資料
- 先依序開啟病人資訊、就診紀錄、報告、用藥、文件完成暖機，再開始計時
- 載入中切換從臨床 tab 首次可見後點擊「就診紀錄」開始，到選取狀態與輕量載入畫面完成第一個 browser paint 為止；該分頁的重內容從下一個畫面週期掛載
- 分頁切換從點擊前開始計時，到分頁成為 active 並完成兩次 browser paint 為止
- 背景恢復測試在同一天依序觸發 `focus` 與 `visibilitychange`，再量測第一次主分頁切換；恢復事件本身也不得產生超過門檻的長工作
- 趨勢開啟從點擊檢驗名稱開始計時，到右側趨勢內容可見並完成兩次 browser paint 為止
- 趨勢關閉從點擊「關閉」開始計時，到右側趨勢隱藏並完成兩次 browser paint 為止；較重的圖表清理由下一個 paint 後執行
- 捲動測試在用藥清單 viewport 每個 animation frame 捲動 120 px
- Long Tasks API 用來觀察超過 50 ms 的主執行緒工作

## 自動防退化測試

測試檔：[`e2e/tests/workspace-performance.spec.ts`](../e2e/tests/workspace-performance.spec.ts)

單輪檢查：

```bash
npx playwright test e2e/tests/workspace-performance.spec.ts --project=chromium --workers=1
```

更新本機基準時連跑五輪：

```bash
npx playwright test e2e/tests/workspace-performance.spec.ts --project=chromium --workers=1 --repeat-each=5
```

每輪都會在 Playwright 結果附加 `performance-metrics` JSON，並在終端輸出同一份數據。CI 使用較寬鬆的上限，是為了容忍共享 runner 的波動，同時攔住秒級分頁卡頓、捲動嚴重掉幀與重複掛載 DOM 等明顯退化。

## 涵蓋與未涵蓋範圍

目前涵蓋：

- 已載入病人後的左側主分頁切換
- 累積報告檢驗趨勢的首次與後續右側開啟
- 右側檢驗趨勢關閉並返回既有功能面板
- 大型用藥清單捲動
- 重複切換後的 DOM 成長
- 操作期間的主執行緒長工作
- 同一天背景恢復事件後的第一次主分頁切換

目前未納入這組 p95：

- 第一次進站與冷啟動編譯
- 遠端 FHIR 載入、院內網路與 SMART OAuth
- AI 模型供應商的回應時間
- 低階院內電腦、iPad 與不同縮放比例的獨立基準

AI 產生時間受模型與網路影響，不應混入 UI p95。AI 摘要仍使用完整組裝後的臨床 context；相關完整性由摘要 use-case 與 generation hook 測試獨立保護，效能測試不以裁切輸入資料換取速度。

每日時間更新只刷新「剩餘天數／目前用藥」等日期語意，不會重新載入或重新解析 Bundle。未來若啟用 SMART on FHIR live launch，應另加來源專屬的更新策略與基準，不讓 live refresh 規則回頭影響本地固定快照。

## 更新規則

1. 影響分頁掛載、虛擬清單、報告 pivot、clinical context 或 AI context 組裝的改動，應執行單輪效能測試。
2. 合併重大效能調整前，應執行五輪基準並更新本文件日期與數字。
3. 若本機 p95 變快但 CI 門檻未變，不需要為了漂亮數字縮緊門檻；先累積不同環境的穩定基準。
4. 若超過門檻，不應直接放寬上限，應先檢查是否發生重複掛載、同步大量運算、版面重排或非必要資料重建。
5. 背景恢復測試必須同時觸發 `focus` 與 `visibilitychange`；同一天恢復不得重建日期相關的完整 UI／AI context，跨日則仍需更新日期語意。
